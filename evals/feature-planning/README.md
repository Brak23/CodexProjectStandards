# Feature planning behavior evaluations

These scenarios define expected and prohibited planner behavior. The dependency-free validator checks their structure. Connect the records to model-specific behavioral harnesses for end-to-end evaluation.

The deterministic integration test also constructs model-v2 workspaces and verifies graph, coverage, evidence, milestone, authorization, and generated-view invariants.
