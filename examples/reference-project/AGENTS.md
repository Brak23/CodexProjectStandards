# Reference project local rules

Follow the root `AGENTS.md`.

For this directory:

- Use only Node.js standard library modules.
- Run `node --test` after behavior changes.
- Keep the HTTP contract dependency-free and observable.
