# Gemini CLI repository instructions

Load the repository operating contract and portable AI-platform controls:

@./AGENTS.md
@./agent-context.yml
@./agent-policy.yml

Use the applicable feature `state.yml`, approved brief, approved plan, and nested `AGENTS.md` files before editing. The imported files are authoritative; this adapter must not weaken or duplicate them.
